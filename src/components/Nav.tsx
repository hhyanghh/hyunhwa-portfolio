import React from "react";
import CustomContainer from "./common/CustomContainer";
import { grey } from "@mui/material/colors";

const Nav = () => {
  return (
    <CustomContainer bgColor={grey[100]}>
      <div>Nav</div>
    </CustomContainer>
  );
};

export default Nav;
