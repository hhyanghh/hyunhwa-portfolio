import React from "react";
import { grey } from "@mui/material/colors";
import CustomContainer from "./common/CustomContainer";

const Main = () => {
  return (
    <CustomContainer bgColor={grey[300]}>
      <div>Main</div>
    </CustomContainer>
  );
};

export default Main;
