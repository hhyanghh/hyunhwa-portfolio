import React from "react";
import CustomContainer from "./common/CustomContainer";
import { grey } from "@mui/material/colors";

const Skills = () => {
  return (
    <CustomContainer bgColor={grey[500]}>
      <div>Skills</div>
    </CustomContainer>
  );
};

export default Skills;
