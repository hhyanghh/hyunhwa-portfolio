import React from "react";
import CustomContainer from "./common/CustomContainer";
import { grey } from "@mui/material/colors";

const Works = () => {
  return (
    <CustomContainer bgColor={grey[600]}>
      <div>Works</div>
    </CustomContainer>
  );
};

export default Works;
